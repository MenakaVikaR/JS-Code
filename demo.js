(join and tostring - converting array to string
push - add last position of an array
pop - remove last postion od an array
push() - return length of an array
pop() - return pop element of an array
shitf() - remove first postion of an array
unshift() - add first position of an array
concat() - used to concet to array
splice() - The splice() method can be used to add new items to an array
eg: fruits.splice(2, 0, "Lemon", "Kiwi");
The first parameter (2) defines the position where new elements should be added (spliced in).

The second parameter (0) defines how many elements should be removed.

fruits.splice(0, 1) -  to remove elements without leaving "holes" in the array

sort() - sorts an array alphabetically
reverse() -  reverses the elements in an array
points.sort(function(a, b){return a - b}); - the sort() function sorts values as strings

